// Defining breakpoints for responsive design
const size = {
	laptopSmall: '1300px',
}

// Media queries for different device sizes
export const device = {
    laptopSmall: `max-width: ${size.laptopSmall}`, // Media query for laptopSmall
}
